<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:int-stream="http://www.springframework.org/schema/integration/stream"
        xmlns:context = "http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/integration
		http://www.springframework.org/schema/integration/spring-integration.xsd
		http://www.springframework.org/schema/integration/stream
		http://www.springframework.org/schema/integration/stream/spring-integration-stream.xsd
                http://www.springframework.org/schema/context 
                http://www.springframework.org/schema/context/spring-context.xsd">
    
<!--        <context:component-scan base-package = "org.mesol.spmes.repo" />
        <context:component-scan base-package = "org.mesol.spmes.service" />-->
 
        <int:channel id="replyChannel"/>
	<int:gateway id="bgate" 
                default-reply-channel="replyChannel"
                service-interface="org.mesol.spmes.service.integration.BeanGateway"/>

	<!-- each order has a collection of order items that is split apart to be processed -->
	<int:channel id="orders"/>
	<int:splitter input-channel="orders" 
                      expression="payload" 
                      output-channel="findEquipment"/>

	<!-- drink order items are aggregated in a call to the waiter -->
	<int:channel id="findEquipment"/>
	<int:service-activator input-channel="findEquipment" 
                               ref="eqService" 
                               method="findByName"
                               output-channel="replyChannel"/>
        
<!--        <int:channel id="preparedDrinks"/>
	<int:aggregator input-channel="preparedDrinks" method="prepareDelivery">
            <beans:bean class="org.mesol.spmes.service.integration.Waiter"/>
	</int:aggregator>-->
        
         <!--output-channel="deliveries"-->
	<!--<int-stream:stdout-channel-adapter id="deliveries"/>-->
	<!--<beans:bean id="eqService" class="org.mesol.spmes.service.EquipmentService"/>-->

	<!--<int:poller id="poller" default="true" fixed-delay="1000"/>-->

</beans:beans>
